/** Figma shell horizontal gutter from the main column edge (sidebar seam). */
const STUDENT_SHELL_GUTTER_CLASS = "px-[24px]"

/** Figma app shell content column (1600px max). */
const STUDENT_PAGE_CONTAINER_CLASS = "mx-auto w-full max-w-[1600px]"

/** Vertical inset for standard student pages below the top bar. */
const STUDENT_MAIN_PADDING_CLASS = "pt-[24px] pb-[24px]"

export { STUDENT_MAIN_PADDING_CLASS, STUDENT_PAGE_CONTAINER_CLASS, STUDENT_SHELL_GUTTER_CLASS }
