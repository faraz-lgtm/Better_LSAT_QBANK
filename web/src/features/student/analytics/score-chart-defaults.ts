/** Drills score-progress chart opens on percent, not PT equivalent. */
export const DEFAULT_DRILL_SCORE_TAB = "percent" as const

/** Section score-progress charts open on raw score, not PT equivalent. */
export const DEFAULT_SECTION_SCORE_TAB = "raw" as const

/** PrepTest score-progress chart opens on scaled PT score, not raw. */
export const DEFAULT_PREPTEST_SCORE_TAB = "scaled" as const
