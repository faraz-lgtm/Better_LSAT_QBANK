import type { CSSProperties } from "react"

/** Panel chrome — tokens in `web/src/index.css` `:root`. */
export const drillSurfaceCard: CSSProperties = {
  borderColor: "var(--greyscale-100)",
  borderWidth: 1,
  boxShadow: "var(--shadow-medium)",
  backgroundColor: "var(--greyscale-25)",
}
